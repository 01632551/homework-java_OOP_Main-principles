package ru.netology.domain;

public class Reposts {
    private int count; // Number of users who copied the post.
    private int userReposted; // Whether the user reposted the post (0 — not reposted, 1 — reposted).
}
